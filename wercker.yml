box: debian
build:
  steps:
    - arjen/hugo-build@1.19.0:
        version: "0.24.1"
        theme: "hugo-steam-theme"
        flags: --buildDrafts=false
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        repo: Xdeon/Xdeon.github.io
        path: public
        domain: hengshi.me
