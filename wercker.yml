box: debian
build:
  steps:
    - arjen/hugo-build@1.11.0:
        version: "0.16"
        theme: hugo_theme_harb
        flags: --buildDrafts=false
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        repo: Xdeon/Xdeon.github.io
        path: public
        domain: hengshi.me
