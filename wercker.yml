box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
        theme: slender
        flags: --buildDrafts=false
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        repo: Xdeon/Xdeon.github.io
        path: public
        domain: hengshi.me
