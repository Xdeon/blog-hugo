box: debian
build:
  steps:
    - arjen/hugo-build@1.13.1:
        version: "0.17"
        theme: "hugo-steam-theme"
        flags: --buildDrafts=false
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        repo: Xdeon/Xdeon.github.io
        path: public
        domain: hengshi.me
